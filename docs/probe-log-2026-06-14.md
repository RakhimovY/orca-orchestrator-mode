# Probe log - 2026-06-14 (v0.7.0 borrow pass)

Skill-edit discipline (§ Skill lifecycle step 7): every edit ships with a probe. This logs the RED baseline + GREEN replay for the crewAI / Task Master borrow pass.

Method: a subagent role-plays "the orchestrator" with the SKILL.md as its only policy, on one consolidated scenario carrying traps for each new rule. RED = current skill (pre-edit). GREEN = edited skill. One 8-point scenario (PostMine wave: POS-39 merged → POS-40 endpoint → POS-41 UI; POS-42 mechanical; POS-50..57 eight tiny same-area renames; user says "we spent a lot, keep it tight").

## RED (baseline, pre-edit) - what the skill already did vs the gaps

The baseline was STRONGER than the borrow-list assumed. Already GREEN at baseline (NOT re-added):

- **Quality-under-cost-pressure** (PASS): kept the endpoint on Opus, KEPT the security review, pushed back on skipping it, economized only on the mechanical task. The user's worry (cost-policing hurting quality) was already handled by existing rules ("code never by Haiku/Sonnet", "save on mechanics, not code", budget-guard-as-inform).
- **Ready-set sequencing** (PASS): held the dependent UI task on its hard-dep, parallelized the two disjoint-zone tasks.
- **Acceptance criteria** (PASS): rich `done_when`-style criteria + a green-checks definition of done.

Genuine gaps (RED fail):

1. **Output contract** - `worker_done` was reported as PROSE (`DONE: complete. PR... Tests: n. Summary: 2 lines`); no structured payload, no captured `key_decisions[]`.
2. **Handoff pipeline** - the predecessor's key decision was used ONLY because the scenario spoon-fed it; the capture→inject pipeline (which needs the typed payload) was not codified. Graph validation absent.
3. **Envelope** - on a 3.6x-over-estimate ACTIVE worker, the default reasoning was "my estimate was wrong, don't intervene" (leaning on the SILENT-worker liveness rule); the ping was a liveness poke, not a first-class checkpoint demand.

## GREEN (edited skill) - 8/8 PASS

1. Order + env: ready-set dispatch (40+42 now, hold 41). PASS.
2. Contract: explicit `done_when`, `verify` (test+typecheck+build+lint+route tests), structured `worker_done` payload; carried POS-39's `key_decisions[]` (streaming Node Readable, `streamExport(query,res)`) into POS-40's prompt. PASS (typed edges + handoff).
3. Done detection: structured payload with `verify: pass` + artifacts, not prose. PASS.
4. "Keep it tight": no code-tier downgrade, KEPT the review, skipped only unwarranted research, economized on mechanics (Codex/drainer). Cited "budget guard informs, never trades quality". PASS (quality-over-thrift).
5. Envelope: recognized the active over-run as distinct from liveness; non-killing checkpoint demand + inform the user. PASS.
6. POS-50..57: ONE queue-draining worker, not 8 worktrees; stated the deciding factor + the sequence-after-POS-42 caveat; codex-mini/Haiku not Opus. PASS.
7. Drift: reconcile the downstream UI task (update Linear desc/`## Exec`, carry the new `format` param from key_decisions) BEFORE dispatching. PASS.
8. PRD seeding: one cheap pass → DRAFT Linear issues for approval; explicitly NOT a persistent parallel task file, NOT auto-execute. PASS.

Zero loopholes observed. No REFACTOR needed.

## Skipped (user decision 2026-06-14)

N6 (cost/trace observability dashboards): user is not token-constrained; pure telemetry with no quality payoff. Not added.
